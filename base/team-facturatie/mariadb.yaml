apiVersion: apps/v1
kind: Deployment
metadata:
  name: facturatie-db
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: facturatie-db
  template:
    metadata:
      labels:
        app: facturatie-db
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 999
        fsGroup: 999
      containers:
      - name: mariadb
        image: mariadb:10.11
        securityContext:
          allowPrivilegeEscalation: false
          capabilities:
            drop:
            - ALL
          readOnlyRootFilesystem: true
        ports:
        - containerPort: 3306
        env:
        - name: MARIADB_ROOT_PASSWORD
          valueFrom:
            secretKeyRef:
              name: shift-secrets
              key: FOSSBILLING_DB_PASS
        - name: MARIADB_DATABASE
          valueFrom:
            secretKeyRef:
              name: shift-secrets
              key: FOSSBILLING_DB_NAME
        - name: MARIADB_USER
          valueFrom:
            secretKeyRef:
              name: shift-secrets
              key: FOSSBILLING_DB_USER
        - name: MARIADB_PASSWORD
          valueFrom:
            secretKeyRef:
              name: shift-secrets
              key: FOSSBILLING_DB_PASS
        livenessProbe:
          exec:
            command: ["/usr/bin/mariadb-admin", "ping", "-h", "localhost"]
          initialDelaySeconds: 30
          periodSeconds: 20
        readinessProbe:
          exec:
            command: ["/usr/bin/mariadb-admin", "ping", "-h", "localhost"]
          initialDelaySeconds: 10
          periodSeconds: 10
        resources:
          requests:
            memory: "256Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        volumeMounts:
        - name: data
          mountPath: /var/lib/mysql
        - name: tmp
          mountPath: /tmp
        - name: run-mysql
          mountPath: /var/run/mysqld
        - name: mariadb-config
          mountPath: /etc/mysql/conf.d/custom.cnf
          subPath: custom.cnf
          readOnly: true
      volumes:
      - name: data
        persistentVolumeClaim:
          claimName: facturatie-db-data-pvc
      - name: tmp
        emptyDir: {}
      - name: run-mysql
        emptyDir: {}
      - name: mariadb-config
        configMap:
          name: facturatie-mariadb-config
---
apiVersion: v1
kind: Service
metadata:
  name: facturatie-db-service
spec:
  selector:
    app: facturatie-db
  ports:
    - port: 3306
      targetPort: 3306
