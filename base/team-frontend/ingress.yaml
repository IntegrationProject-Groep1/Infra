apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: frontend-ingress
  namespace: shift-festival
  annotations:
    kubernetes.io/ingress.class: "nginx"
spec:
  rules:
  - host: frontend.shiftfestival.be # Vervang door het werkelijke domein
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: frontend-drupal-service
            port:
              number: 80
