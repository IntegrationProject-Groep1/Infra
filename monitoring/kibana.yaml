apiVersion: apps/v1
kind: Deployment
metadata:
  name: kibana
spec:
  replicas: 1
  selector:
    matchLabels:
      app: kibana
  template:
    metadata:
      labels:
        app: kibana
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        fsGroup: 1000
      initContainers:
      - name: setup-kibana-user
        image: curlimages/curl:8.7.1
        securityContext:
          allowPrivilegeEscalation: false
          capabilities:
            drop:
            - ALL
          readOnlyRootFilesystem: true
        env:
        - name: ELASTIC_PASSWORD
          valueFrom:
            secretKeyRef:
              name: shift-secrets
              key: ELASTIC_PASSWORD
        command:
        - sh
        - -c
        - |
          until curl -s -u "elastic:${ELASTIC_PASSWORD}" http://elasticsearch-service:9200; do
            echo "Waiting for Elasticsearch..."
            sleep 5
          done
          echo "Elasticsearch is up. Setting up kibana_system user..."
          curl -s -u "elastic:${ELASTIC_PASSWORD}" -X POST "http://elasticsearch-service:9200/_security/user/kibana_system/_password" \
            -H "Content-Type: application/json" \
            -d "{\"password\":\"${ELASTIC_PASSWORD}\"}"
        volumeMounts:
        - name: tmp
          mountPath: /tmp
      containers:
      - name: kibana
        image: docker.elastic.co/kibana/kibana:8.17.0
        securityContext:
          allowPrivilegeEscalation: false
          capabilities:
            drop:
            - ALL
          readOnlyRootFilesystem: false # Kibana heeft veel schrijfbare mappen nodig
        ports:
        - containerPort: 5601
        env:
        - name: ELASTICSEARCH_HOSTS
          value: "http://elasticsearch-service:9200"
        - name: ELASTICSEARCH_USERNAME
          value: "kibana_system"
        - name: ELASTICSEARCH_PASSWORD
          valueFrom:
            secretKeyRef:
              name: shift-secrets
              key: ELASTIC_PASSWORD
        resources:
          requests:
            memory: "512Mi"
            cpu: "100m"
          limits:
            memory: "1Gi"
            cpu: "500m"
        volumeMounts:
        - name: tmp
          mountPath: /tmp
        - name: kibana-data
          mountPath: /usr/share/kibana/data
      volumes:
      - name: tmp
        emptyDir: {}
      - name: kibana-data
        emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: kibana-service
spec:
  selector:
    app: kibana
  ports:
    - port: 5601
      targetPort: 5601
      nodePort: 30061
  type: NodePort
