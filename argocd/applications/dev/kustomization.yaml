apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Only dev-app.yaml — prod-app.yaml is intentionally excluded.
# The root ArgoCD app (root-app.yaml) points here so it never touches prod.
resources:
  - dev-app.yaml
