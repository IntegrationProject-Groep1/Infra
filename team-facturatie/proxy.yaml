apiVersion: apps/v1
kind: Deployment
metadata:
  name: facturatie-proxy
spec:
  replicas: 1
  selector:
    matchLabels:
      app: facturatie-proxy
  template:
    metadata:
      labels:
        app: facturatie-proxy
    spec:
      containers:
      - name: nginx
        image: nginx:alpine
        ports:
        - containerPort: 80
        volumeMounts:
        - name: config
          mountPath: /etc/nginx/conf.d/default.conf
          subPath: default.conf
          readOnly: true
      volumes:
      - name: config
        configMap:
          name: facturatie-nginx-config
---
apiVersion: v1
kind: Service
metadata:
  name: facturatie-proxy-service
spec:
  type: NodePort
  selector:
    app: facturatie-proxy
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30010
